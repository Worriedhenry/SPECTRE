export const ChangeName = (name) => {
    return {
        type: "CHANGENAME",
        payload: name,
    }
}
export const ChangeAge = (name) => {
    return {
        type: "CHANGEAGE",
        payload: name,
    }
}